# =============================================================================
# Environment Variables Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)
# =============================================================================

# OpenAI Configuration
# --------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o


# AWS Configuration
# -----------------
# Get credentials from AWS IAM Console: https://console.aws.amazon.com/iam/
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_DEFAULT_REGION=eu-central-1


# S3 Storage
# ----------
# The S3 bucket name where embeddings are stored
S3_BUCKET=your_s3_bucket_name_here
S3_EMBEDDINGS_KEY=talk2data/embeddings/embeddings.json


# Embedding Model
# ---------------
# OpenAI embedding model to use (default: text-embedding-3-small)
EMBEDDING_MODEL=text-embedding-3-small


# =============================================================================
# Optional Configuration
# =============================================================================

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Maximum input length for user queries
# MAX_QUERY_LENGTH=500

# Number of top matches to return from similarity search
# TOP_K_MATCHES=3


# =============================================================================
# Setup Instructions
# =============================================================================
# 
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Get OpenAI API Key:
#    - Go to https://platform.openai.com/api-keys
#    - Create new secret key
#    - Copy and paste into OPENAI_API_KEY
#
# 3. Get AWS Credentials (if using S3):
#    - Go to AWS IAM Console
#    - Create access key for your user
#    - Copy Access Key ID and Secret Access Key
#    - Set appropriate region (e.g., eu-central-1, us-east-1)
#
# 4. Create S3 Bucket (if needed):
#    - Go to AWS S3 Console
#    - Create new bucket
#    - Copy bucket name to S3_BUCKET
#
# 5. IMPORTANT: Never commit .env to Git!
#    (Already in .gitignore)
#
# =============================================================================
